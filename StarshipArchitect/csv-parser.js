function parseCSVLine(line){const fields=[];let current="",inQuotes=!1;for(let i=0;i<line.length;i++){const char=line[i];'"'===char?inQuotes=!inQuotes:","!==char||inQuotes?current+=char:(fields.push(current),current="")}return fields.push(current),fields}function parseCSV(csvContent){const lines=csvContent.trim().split(/\r?\n/);if(lines.length<2)throw new Error("CSV must have at least a header row and one data row");const dataLines=lines.slice(1),components=[];let currentCategory="",totalTons=0,totalCost=0;for(const line of dataLines){const fields=parseCSVLine(line);if(fields.length<4)continue;let[category,item,tons,cost]=fields;if(category=category.trim(),item=item.trim(),tons=parseFloat(tons.replace(/[^0-9.-]/g,""))||0,cost=parseFloat(cost.replace(/[^0-9.-]/g,""))||0,category?currentCategory=category:category=currentCategory,"total"===category.toLowerCase())totalTons=tons,totalCost=cost;else{let quantity=1,itemName=item;const quantityMatch=item.match(/^(.+?)\s*[xX×]\s*(\d+)$/);quantityMatch&&(itemName=quantityMatch[1].trim(),quantity=parseInt(quantityMatch[2]));const tonsPerItem=tons/quantity;components.push({category:category,item:item,itemName:itemName,tons:tons,tonsPerItem:tonsPerItem,cost:cost,quantity:quantity})}}return{components:components,totalTons:totalTons,totalCost:totalCost}}