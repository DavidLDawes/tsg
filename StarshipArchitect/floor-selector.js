function setupFloorSelector(){const selector=document.getElementById("floor-selector");if(!selector)return;const currentValue=selector.value;selector.innerHTML='<option value="all">All</option>';for(let i=1;i<=shipData.numFloors;i++){const option=document.createElement("option");option.value=i,option.textContent=`Floor ${i}`,selector.appendChild(option)}if("all"!==currentValue){const floorNum=parseInt(currentValue);floorNum>=1&&floorNum<=shipData.numFloors?selector.value=currentValue:selector.value="all"}filterFloorDisplay(selector.value)}function setFloorSelector(floorNumber){const selector=document.getElementById("floor-selector");selector&&(selector.value=String(floorNumber),filterFloorDisplay(String(floorNumber)))}function filterFloorDisplay(selectedValue){const floorItems=document.querySelectorAll(".floor-item");if("all"===selectedValue)floorItems.forEach(item=>{item.style.display=""});else{const selectedFloorNum=parseInt(selectedValue);floorItems.forEach((item,index)=>{const floorNum=index+1;item.style.display=floorNum===selectedFloorNum?"":"none"})}}function initializeFloorSelector(){const selector=document.getElementById("floor-selector");selector&&selector.addEventListener("change",event=>{filterFloorDisplay(event.target.value)})}